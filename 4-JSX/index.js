let page= <div>
    <h1>This is Jsx</h1>
    <p>Div is a parent and h1 and p are inside the parent elements</p>
    <p>this is syntex, we cant use two siblings without a parent element</p>
</div>

ReactDOM.render(page, document.getElementById('root'))